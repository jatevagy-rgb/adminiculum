// Thin routing layer — delegates to the canonical wrapped review surface.
// All review logic lives in ReviewPageContent.tsx.
import WrappedReviewPage from "./ReviewPageContent";
export default WrappedReviewPage;
