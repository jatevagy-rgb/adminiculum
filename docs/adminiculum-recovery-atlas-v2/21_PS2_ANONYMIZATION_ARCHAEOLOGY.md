# Anonymization archaeology

Anonymization and rehydration are canonical, security-sensitive capabilities with backend modules, DTO shaping, object/case authorization, and frontend modal flows. SEC-0B1 hardened PII boundaries and response shaping.

Historical implementations are evidence of semantics only. Do not recover a version that leaks PII, bypasses case authorization, mutates identities unsafely, or reintroduces raw provider/error content. The current Word-primary flow is the recovery baseline.
