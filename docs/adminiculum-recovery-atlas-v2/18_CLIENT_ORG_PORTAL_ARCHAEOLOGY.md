# Client, organization, and portal archaeology

Portal history runs from read-only alpha (`836d256`) through workspace routes/aggregate, identity/membership, onboarding/approval, publication, CP1 organizational intake, Phase 5 organization shell, company workspace, and current customer-safe projections.

The identity and portal recovery line must be evaluated against exact issuer/subject binding, no synthetic identity mutation, workforce/customer separation, grant-scoped access, safe DTOs, and truthful unsupported-mode states. Historical grant-to-browse convenience was intentionally reduced where it weakened object authorization; it is not automatically a regression.

The main product cut is organization/client intake → case grant/publication. Do not infer that every internally created case is portal-visible.
