# Time and capacity archaeology

Time entry CRUD, case-aware time pages, presets, reports, saved instances, DOCX output, dashboard workload summaries, attention ordering, and capacity/read-model branches are all evidenced. `d49d410`, `5f7ec2c`, and current timesheet/workload modules support `BUILT_SOMEWHERE_IN_HISTORY`.

The product gap is connective: task completion, case context, agenda/deadline, and time attribution do not form one proven end-to-end path everywhere. Billing remains unproven/greenfield and must not be fabricated from timesheet data.
