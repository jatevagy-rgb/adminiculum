# Lost but useful capabilities

## Case → Work Package on modern creation

`BEST_HISTORICAL_SHA=9eec7bf`; `BEST_HISTORICAL_PR=PR87 lineage`; snapshot/runtime descendants are PR96/PR98. It instantiated work scope and task structure. The modern compact flow did not consistently call the canonical snapshot path. `CURRENTLY_WEAKER=YES`, `RECOVERABLE=YES`, `SECURITY_SAFE_TO_RECOVER=YES` only through current services and authorization. `RECOMMENDATION=MERGE_SEMANTICS` after PR96, then PR98.

## DOCX/PDF text comparison

`BEST_HISTORICAL_SHA=509412d`; `BEST_HISTORICAL_PR=PR94`. Shared extraction and typed diff infrastructure exists in the recovery branch; canonical comparison remains constrained/gated. `CURRENTLY_WEAKER=YES`, `RECOVERABLE=YES`, `SECURITY_SAFE_TO_RECOVER=YES` after format/size/storage gates. `RECOMMENDATION=REPLAY`.

## Contextual communication read model

`BEST_HISTORICAL_SHA=b36113a`; `BEST_HISTORICAL_PR=peterfi communication stack`. Client/case context, fail-closed dual-link authorization, effective timestamps, and no-N+1 projection are stronger than the older isolated surface. `CURRENTLY_WEAKER=YES`, `RECOVERABLE=YES`, `SECURITY_SAFE_TO_RECOVER=YES` with current auth repair preserved. `RECOMMENDATION=RECONNECT`.

## Internal intake → portal publication

Historical portal publication is proven, but internally created cases do not automatically receive the same grant/publication semantics. `CURRENTLY_WEAKER=YES`, `RECOVERABLE=YES`, `SECURITY_SAFE_TO_RECOVER=NO` until explicit grant policy and object authorization are designed. `RECOMMENDATION=MERGE_SEMANTICS`, not cherry-pick.

## Intake typed deadline → agenda

Typed intake deadlines and agenda/case deadline services are separately present. The mapping is not exhaustively proven. `CURRENTLY_WEAKER=UNKNOWN`, `RECOVERABLE=YES`, `SECURITY_SAFE_TO_RECOVER=YES`. `RECOMMENDATION=RECONNECT` after a focused contract test.

## Old editor autosave/track changes

Historical editor generations include working-copy semantics. Current immutable `DocumentVersion` survives, and the browser Word-clone direction conflicts with frozen principles. `CURRENTLY_WEAKER=NO` for the intended product, `RECOVERABLE=NO`, `SECURITY_SAFE_TO_RECOVER=NO`. `RECOMMENDATION=DO_NOT_RECOVER`.

## Old SharePoint-folder move behavior

V1 case-status folder movement is superseded by current storage architecture. `RECOVERABLE=NO`; `RECOMMENDATION=DO_NOT_RECOVER`.
