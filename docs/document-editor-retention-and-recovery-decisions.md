# Document Editor Retention and Recovery Decisions

## Current decision

No stored editor source exists today, so there is no editor-content retention or recovery workflow to claim.

## Blockers for real persistence

Future approval must decide storage location, retention duration, archive behavior, deletion behavior, backup/recovery expectation, restored-version history behavior, and case-archive access behavior.

## Explicit non-decisions

This task did not approve physical deletion, SharePoint version restore, DB JSON storage, browser durable storage, temporary backend memory storage, external converters, or Client Portal exposure.
